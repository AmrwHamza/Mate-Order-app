import 'package:flutter/material.dart';

class MyLocationBody extends StatelessWidget {
  const MyLocationBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}